#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen- length of string
 * @s: parameter
 * Return: length of string
 */

int _strlen(char *s)
{
int len;

len = strlen(s);

return (len);

}
