#include <stdio.h>
/**
 *main- using the if statement
 *Description: number positive,negative or zero
 *Return: 0
 */
int main(void)
{
char n;
for (n = 'a' ; n <= 'z' ; n++)
{
	putchar(n);
}
putchar('\n');
return (0);
}
